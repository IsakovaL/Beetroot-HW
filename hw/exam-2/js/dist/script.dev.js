"use strict";

// $('.single-item').slick();
$(document).ready(function () {
  $('.slick-slider').slick({
    // infinite: true,
    // dots:true,
    arrows: false,
    autoplay: true,
    autoplaySpeed: 1000
  });
});